-- All services share one database (separate schemas/tables). Tables are created
-- by Hibernate (ddl-auto=update). This file just ensures the database exists.
SELECT 'pedidos360 database ready' AS status;
